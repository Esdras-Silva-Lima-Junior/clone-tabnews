function Home() {
  return <h1>Hello world!!!</h1>;
}

// Para o Next.js saber qual função deve ser exportada e renderizada, pra isso precisamos informar com o comando export qual a função que queremos usar na exibição, para podermos definir a exportação, precisamos usar o comando "export default *NOME DA FUNÇÃO*;".

export default Home;
